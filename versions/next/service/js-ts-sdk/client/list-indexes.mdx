---
title: "List Indexes"
mode: "wide"
noindex: true
---

Returns the names of all indexes on the CyborgDB service.

```typescript
async listIndexes(): Promise<string[]>
```

### Returns

`Promise<string[]>`: index names. Empty when the service has no indexes.

### Exceptions

| Exception | Thrown when |
|-----------|-------------|
| `CyborgDBAuthenticationError` | The client's `apiKey` is missing or invalid (HTTP `401`). |
| `CyborgDBServiceError` | The service returns `5xx`. |
| `Error` | The service can't be reached. See [Known gaps](../errors#known-gaps). |

See [Errors](../errors) for the full hierarchy.

### Example Usage

```typescript
import { Client } from 'cyborgdb';

const client = new Client({ baseUrl: 'http://localhost:8000', apiKey: 'your-api-key' });

const indexes = await client.listIndexes();
console.log(indexes);
// Output: [ 'documents', 'embeddings', 'semantic-search' ]
```
