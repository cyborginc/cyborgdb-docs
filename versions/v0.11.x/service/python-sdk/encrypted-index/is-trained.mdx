---
"title": "Is Trained"
"mode": "wide"
---

Checks whether the index has been trained.

```python
index.is_trained()
```

### Parameters

This method does not require any parameters.

### Returns

`bool` - Returns `True` if the index has been trained, otherwise `False`.